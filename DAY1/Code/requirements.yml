# Ansible Collections 依赖清单
# 用法：ansible-galaxy collection install -r requirements.yml -p ./collections
collections:
  - name: cisco.ios
    version: 9.2.0
  - name: ansible.netcommon
    version: 7.2.0
  - name: ansible.utils
    version: 5.1.2
